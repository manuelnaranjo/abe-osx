metadata:
  name: bench-session-kvm
  format: "Lava-Test-Shell Test Definition 1.0"
  description: "Benchmarking Session"
  version: 1.0

params:
    PUB_KEY: None
    CONFIG: None

install:
    deps:
        - openssh-server
        - make
        - cpufrequtils
        - binutils
        - gawk
        - libgfortran3
        - xz-utils

run:
    steps:
        - ./config/bench/lava/setup "$PUB_KEY"
        - ./config/bench/lava/target-session "$CONFIG"
